#include <stdio.h>
#define SIZE  10

int main (void) {

    // $200 per week + 9% of gross sales/week
    // e.g: $3000 gross sale in the week;

    //      $200 + $(3000 * %9)  >>> receives $470

    // use one-dimensional array
    // use array of counters

    int counters[SIZE] = {0};

    for(size_t i = 0; i < SIZE; ++i) {


    }
}